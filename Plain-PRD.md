# 카테캠 주간 트래커 — PRD

> 채워 넣는 용도의 템플릿. 각 섹션에 내용 작성.

## 배경 및 목적
 본 프로젝트는 현재 Discord / Notion에 흩어져 있는 팀 및 개인 일정의 추적 및 완료 상태를 추적하는 웹사이트를 개발하는 프로젝트이다. 일정 웹사이트를 공유함으로서 접근성을 높여 쿠키즈(카테캠 학생)가 일정을 까먹고 수행하지 않는 케이스를 줄이고, 더 체계적으로 계획을 수행하는 것을 보조하는 것을 목표로 한다. 현재로서는 Discord / Notion 일정 크롤링을 위한 권한이 없으므로, 일단은 개발자가 수동으로 크롤링하여 텍스트화 시키면 이를 LLM이 인지하고 일정을 추가 및 수정하는 것으로 한다.
 현재 이 웹사이트는 일정 추적만을 위한 것으로, 커뮤니케이션 기능은 타인의 일정 확인 외에는 일절 고려하지 않는다. (Discord, Notion과 중복됨)

## 타겟 사용자
 본 프로젝트의 사용자는 크게 세 종류로 분류한다.
 1. 개발자 본인
 - 웹사이트 및 서비스를 유지 보수한다.
 - LLM이 일정을 추가하기 전에 확인하고 추가를 지시한다.
 - 만약 잘못된 일정이 추가되었거나 롤백을 해야하는 상황이 생긴다면 이를 처리하는 역할을 한다.
 2. 운영진
 - LLM이 놓친 일정이나, 직접적으로 공유 일정(하단에서 설명)을 추가 할 수 있다.
 - 학생/팀의 일정 상황을 한 눈에 확인할 수 있다.
 3. 학생
 - 해당 웹사이트의 주요 Client이다.
 - 카테캠 전체 공유 일정을 확인하고 진행 상황을 체크할 수 있다.
 - 본인 개인 일정을 추가하고 진행 상황을 체크할 수 있다.
 - 자신의 '팀'의 일정들을 확인할 수 있다.

## 서비스 내용

### 핵심 기능(우선순위 순)
이하에서 CRUD는 알파벳으로 줄여 표현

0. 공통
- 이메일 기반 인증 및 로그인 기능(User CRUD)
- 공유 일정 R 및 개인 진행 상황 U 기능
- 개인 일정 CRUD

1. 사용자
- 공통

2. 운영진
- 공유 일정 CRUD
- 할당된 학생 일정 R
- 전체 팀단위 일정 R

3. 개발자
- 사용자/운영진의 권한을 모두 포함한다.
- 전체 계정 CRUD 기능
- 최근 Discord/Notion 업데이트 사항 크롤링 및 자동 요약
- Agent가 요약한 일정 추가 계획 R 및 C/U 지시
- Agent 자동 C/U 전 백업파일 생성, 필요시 백업 수행

### MVP 범위 (이번 단계에서 할 것)
- 이메일 인증 및 로그인 기능
- 공유 일정 / 개인 일정 CRUD
- 특정 User 일정 조회(권한 분리 필요)
- 크롤링 텍스트 기반 일정 추가 계획 수립 (현재는 수동으로 텍스트 입력)
- 계획 Approve 후 실제 추가 기능
- 추가 전 백업파일 생성

### MVP 이후 / 나중에 (지금은 안 할 것)
- Discord/Notion 크롤링, 자동 요약 기능

## 사용자 시나리오 / 플로우

### 개발자/Agent
- Agent는 한국 기준 매일 정오, 디스코드의 주요 공지 채팅을 크롤링하여 추가된 사항이 있다면, 이를 분석하여 공유 일정을 자동으로 추가한다.
- 개발자 및 운영진을 이를 주기적으로 확인하며 오작동한 일정이 있다면 수정한다.
- Agent는 CR은 자유롭게 가능하나... UD는 가급적 하지 않는 방향으로 한다.(혼란 유발 가능성 높음)

### 운영진
- Agent가 등록한 공유일정을 수정/삭제하거나 직접 공유 일정을 추가하여 학생들 모두가 보도록 할 수 있다.
- 학교 별로 팀을 생성하여 학생을 할당할 수 있다.
- 특정 학생/팀의 일정 상황을 조회할 수 있다.

### 학생
- 이메일 인증 후 계정을 생성하여 로그인한다.
- 공유 일정은 자동으로 달력에 추가되며, 자신의 진행상황을 업데이트할 수 있다.
- 자신의 개인일정을 등록할 시, 달력에 추가되며, 진행상황을 업데이트할 수 있다.
- 만약 자신이 속한 팀의 일정이 추가된다면 자동으로 개인 달력에 추가된다.


## 기술 스택

### 백엔드
FastAPI


### 프론트엔드
React 혹은 Next.js

### 데이터베이스
PostgreSQL(AWS EC2 Docker) -> RDS 이미그레이션

### 인증/권한
JWT, email 인증으로 중복 가입 방지

### 외부 연동 (디스코드 API, 노션 API, Claude API 등)
Agent: Claude API 사용
- 디스코드 봇, 노션 API: 운영진과 논의 후 권한 취득 예정

### 배포/호스팅
AWS EC2/RDS/S3(필요시 추가)

## 데이터 모델 (초안)

### User
- User_id: Integer
- email: str
- password: str (단방향 암호화)
- nick_name: str | None
- permission: Literal['dev','manager','student']

### Shared_Schedule
- Schedule_id: Integer
- title: str
- contents: str
- deadline: DateTime 
- done_student_ids: list[Integer]

### Team_Schedule
- Schedule_id: Integer
- team_id: Integer
- title: str
- contents: str
- deadline: DateTime
- status: Literal['in_progress','done']

### Personal_Schedule
- Schedule_id: Integer
- Student_id: Integer
- title: str
- contents: str
- deadline: DateTime
- status: Literal['in_progress','done']

## 제약 사항

### 권한/보안
- 이메일 인증을 통해 중복 가입을 방지한다.
- 비밀번호는 단방향 암호화로 복호화를 불가하게 한다.
- 로그인 인증은 JWT를 사용한다.
- 모든 id 부여는 순차적이지 않고, 랜덤 생성을 사용하여 중복되지 않고 예측 불가능 하도록 한다.
- 동일 사용자로부터 비정상적으로 많은 양(초당 5번 이상 등)이 온다면 해당 계정을 잠시 블락한다.

### 성능/비용
- 단순 API 콜이고, 사용자의 수가 200명을 넘지 않으며, 많은 동시 요청을 할 경우가 없으므로 크지 않을 것으로 예상.

### 기타
- 크롤링/요약 및 추가 계획 수립은 Agent를 사용

## 성공 지표 (이 서비스가 잘 되고 있다고 어떻게 판단할지)


## 오픈 이슈 / 결정 필요 사항

